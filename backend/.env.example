# CF-Nav 后端环境变量配置示例
# 复制此文件为 .env 并填写实际值

# ============================================
# JWT 配置
# ============================================
# JWT 密钥（必须）- 请使用至少 32 位的随机字符串
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-me-to-random-string-at-least-32-chars

# ============================================
# CORS 配置
# ============================================
# 允许的前端域名列表，多个域名用逗号分隔
# 开发环境示例
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# 生产环境示例（部署时替换为实际域名）
# ALLOWED_ORIGINS=https://your-domain.pages.dev,https://yourdomain.com

# ============================================
# 数据库配置（Cloudflare D1）
# ============================================
# 注意: D1 数据库在 wrangler.toml 中配置
# 本地开发会自动使用 .wrangler/state/v3/d1/ 下的 SQLite 文件

# ============================================
# 其他配置
# ============================================
# Node 环境
NODE_ENV=development

# API 版本
API_VERSION=v1
